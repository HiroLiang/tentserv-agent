# v1.x Roadmap

Status: active long-term product roadmap after the `v1.0.0` stability release.

This roadmap tracks post-1.0 product evolution. It is for new capabilities,
architecture direction, and version-sized feature slices. Bug fixes, release
follow-up, and documentation cleanup belong in
[bugfix-maintenance-plan.md](./bugfix-maintenance-plan.md).

## Purpose

`v1.0.0` froze the documented stable compatibility promise. The `v1.x` line
should now add capabilities without weakening that promise:

- keep stable CLI and daemon REST behavior predictable
- keep provider-compatible support explicit and partial where documented
- make model, adapter, backend, runtime profile, and platform compatibility
  explainable before routing grows more dynamic
- add multi-model serving only when each configured route can be validated and
  inspected

## Roadmap Principles

- Keep the product surface CLI plus daemon REST.
- Prefer vertical feature slices that carry one operator-visible capability
  through contracts, domain behavior, entry points, docs, and tests.
- Keep native Tentgent request intent separate from provider-shaped payloads.
- Treat static model and adapter metadata as hints, not proof.
- Treat local verification on this machine as authoritative. Shared or built-in
  records can provide hints, but they must not replace local evidence.
- Avoid hidden fallback to another model, backend, provider, or generated
  metadata when a configured capability is missing.
- Keep large artifacts in filesystem stores; add indexed metadata only when it
  improves compatibility, proof, routing, or query behavior.

## v1.1 Candidate Scope

`v1.1.0` should focus on the smallest useful multi-model serving foundation:

- define a cluster as the user-facing named routing object, with route targets
  kept as internal cluster fields rather than independent public refs
- persist cluster definitions as canonical TOML under
  `TENTGENT_HOME/clusters/<cluster_ref>/cluster.toml`
- validate configured `chat`, `embedding`, `rerank`, `audio-transcription`,
  and `vision-chat` routes against known local model capability and runtime
  profile state
- add read-only cluster inspection before broad lifecycle automation
- start with cluster server routing across `chat`, `embedding`, `rerank`,
  `audio-transcription`, and `vision-chat`, using the existing server
  lifecycle instead of creating a separate cluster lifecycle
- preserve existing dynamic request-time adapter selection instead of storing
  fixed `model + adapter` cluster targets
- add only the resource gates needed to avoid obvious multi-model conflicts
- keep unsupported provider-compatible and multimodal requests on clear errors

The first cluster slice should not implement automatic multimodal context
assembly, provider tool orchestration, OpenShell broker routing, shared
registries, or conversion automation.

The selected cluster slices are tracked in the focused
[cluster-roadmap.md](./cluster-roadmap.md) sub-roadmap.

## Current Handoff State

The post-`v1.0.0` patch queue in
[bugfix-maintenance-plan.md](./bugfix-maintenance-plan.md) has no open tracked
issues after `#107`. GitHub issues `#113`-`#118` now track the selected
`v1.1.0` cluster slices. Implementation slices `#114`-`#117` are complete.
Slice `#118` remediation is implemented for runtime lifecycle, mutation
stabilization, platform replacement, focused ownership diagnostics, module
structure, interface injection, typed result projection, and deterministic
watcher verification. Findings `R1`-`R19` pass the final local verification
matrix, including the declared Rust 1.81 minimum, and GitHub tracking matches
the active work. Native Windows CI rerun remains pending, so parent `#113`
closeout and release-scope verification remain blocked. A new
session without
prior conversation context should review
[cluster-roadmap.md](./cluster-roadmap.md), the
[cluster runtime ownership plan](./cluster-runtime-ownership-plan.md), and the
[active remediation plan](./cluster-runtime-ownership-remediation.md) before
changing status or archiving cluster records.

## v1.1 Issue Continuation Bootstrap

Use this sequence when starting `v1.1.0` from a clean session:

1. Confirm the native Windows CI gate for `#118`; parent `#113` closeout must
   not start yet.
2. Read this roadmap plus
   [cluster-roadmap.md](./cluster-roadmap.md).
3. Confirm the GitHub milestone `v1.1.0` and project board fields are still
   attached to the issue.
4. Confirm decisions `29`-`33`, review the implemented `R1`-`R8` evidence, and
   verify the combined `#114`-`#118` completion checks. Do not create another
   cluster slice unless the roadmap is intentionally reopened.
5. Use labels that already exist in this repository when opening any follow-up:
   `enhancement`, `type:tracking`, `type:implementation`, `type:audit`,
   `area:roadmap`, `area:api`, `area:gating`,
   `area:model-support`, `area:runtime-profile`, and `area:diagnostics` as
   applicable.
6. When a follow-up branch is needed, create it only after the issue scope and
   order are accepted.

Do not create a new storage migration, shared registry, provider tool
orchestration, multimodal context system, or conversion automation issue as a
`v1.1.0` item unless the cluster sub-roadmap is updated first.

## Compatibility Foundations

The cluster direction depends on compatibility work that may be implemented as
part of `v1.1.0` or split into closely related slices:

- durable local compatibility proof storage for model, capability, backend,
  runtime package, platform/device class, adapter, and input/output shape
- tuple-aware model and LoRA compatibility gates so one successful adapter or
  model launch is not trusted for unrelated tuples
- actionable inspect and doctor output for verified, failed, stale, unknown, and
  unsupported local tuples
- bounded dynamic runtime routing where candidate attempts are documented,
  cached, and inspectable
- resource coordination for model mutation, adapter writes, conversion, cache
  updates, warm server ownership, and GPU/CPU/memory-sensitive jobs

## v1.x Module Flow

Use this flow to decide whether a candidate belongs in `v1.1.0`, a later `1.x`
feature release, or the maintenance plan.

| Module | Role In The System | v1.1 Position | Notes |
| --- | --- | --- | --- |
| Bugfix And Maintenance | Patch fixes, stale docs, diagnostics polish, release follow-up, and repository hygiene for already released behavior. | Not part of the feature roadmap. Track in [bugfix-maintenance-plan.md](./bugfix-maintenance-plan.md). | Use patch milestones such as `v1.0.1 Patch`; move work here only when it requires a new public surface or version-sized design. |
| Cluster System | User-facing cluster object that owns internal route targets for configured capabilities such as `chat`, `embedding`, `rerank`, `audio-transcription`, and `vision-chat`. | Primary `v1.1.0` candidate. | The focused plan lives in [cluster-roadmap.md](./cluster-roadmap.md). Start with state safety, cluster definition, route readiness, native local routing, dynamic adapter compatibility, and minimal runtime ownership. |
| Compatibility State System | Route readiness, durable proof concepts, tuple-aware model/LoRA gates, support status, runtime profile visibility, and runtime failure evidence. | Define with cluster in `v1.1.0`; implement only the minimum needed for cluster slices. | SQLite or an equivalent indexed metadata layer is the likely long-term backend, but migration should happen incrementally by state family instead of as one broad storage rewrite. |
| Runtime Resource System | Delete protection, active route ownership, cancellation, shutdown, cleanup, and minimal resource gates for multi-model local serving. | Minimal cluster-required subset in `v1.1.0`; broader scheduler is not in `v1.1.0`. | Keep early work limited to protecting bound resources and active cluster routes. Defer cross-job scheduling and broad GPU/CPU policy. |
| Dynamic Runtime Routing | Bounded candidate backend/runtime selection when one model may work through multiple runtime families. | Not in `v1.1.0` unless required for a selected cluster route. | Keep attempts bounded, cached, and inspectable. Do not add hidden fallback routing. |
| Multimodal Context System | Convert files, images, audio, or video into bounded context artifacts before chat dispatch. | Not in `v1.1.0`. | Depends on stable cluster routing, route readiness, and resource ownership. |
| Provider Orchestration System | Provider tool/function-call mapping, provider-specific extensions, and cloud rerank adoption. | Not in `v1.1.0`. | Keep provider-shaped behavior explicit at ingress/egress; do not pass through unsupported provider fields silently. |
| External Trust And Conversion System | OpenShell broker auth, shared compatibility registry, explicit model conversion, and generated metadata provenance. | Not in `v1.1.0`. | Local verification remains authoritative; conversion must be an explicit user action, not an execution-time patch. |

## Later 1.x Work

These directions are intentionally kept out of the first `v1.1.0` cluster MVP
unless a later planning pass narrows them into a safe vertical slice:

- automatic multimodal context assembly for files, images, audio, and video
- provider-compatible tool and function-call orchestration
- cloud rerank as an explicit provider or adapter family
- OpenShell-managed provider gateway or broker auth boundaries
- shared compatibility registry publish/consume behavior
- explicit conversion and metadata-generation workflows with provenance
- broader direct media server wrappers and typed streaming contracts beyond the
  already stable route families

## Issue Planning Rules

- Put feature and architecture issues here when they change product capability
  or public workflow shape.
- Put patch fixes, stale docs, release follow-up, and diagnostics polish in
  [bugfix-maintenance-plan.md](./bugfix-maintenance-plan.md).
- Use milestones for release targeting, such as `v1.1.0` or `v1.2.0`.
- Use the GitHub Project board for status, priority, area, and work type.
- Archive or rewrite this plan when its main tracks are completed or
  superseded.
